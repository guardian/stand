/**
 * UserFeedbackSummary component entry point
 *
 * Peer dependencies required to use these components:
 * - `@emotion/react`
 * - `react`
 * - `react-dom`
 * - `typescript`
 *
 * See the `peerDependencies` section of package.json for compatible versions.
 *
 * If you only need the built CSS (./component/userFeedbackSummary.css),
 * you don't need to install these.
 */
export { UserFeedbackSummary } from './components/UserFeedbackSummary/UserFeedbackSummary';
export type { UserFeedbackSummaryProps } from './components/UserFeedbackSummary/types';
export type { PartialUserFeedbackSummaryTheme as UserFeedbackSummaryTheme } from './components/UserFeedbackSummary/styles';
export { componentUserFeedbackSummary } from './styleD/build/typescript/component/userFeedbackSummary';
export type { ComponentUserFeedbackSummary } from './styleD/build/typescript/component/userFeedbackSummary';
