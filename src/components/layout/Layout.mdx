import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import LayoutStories, {
	AlertBannerTopBarGridSidebarAboveGridSm,
	CssOverrides,
	CustomTheme,
	TopBarGrid,
	TopBarGridSidebarHiddenSm,
	TopBarGridSidebarAboveGridSm,
} from './Layout.stories';
import {
	SandboxReact,
	SandboxCss,
	SandboxJs,
} from '../../util/storybook/sandbox/Sandbox';
import {
	componentName,
	componentCss,
	componentHtml,
	componentJs,
	componentTsx,
} from './sandbox';

<Meta of={LayoutStories} />

# Layout

Layout is a page-shell component that composes `AlertBanner`, `TopBar`, `Sidebar`, and `Grid` into a responsive layout.

It provides consistent structure across tools while allowing each region to be populated with existing Stand components.

## When to use

- When your page needs a standard shell and layout with a top bar and main content grid.
- When you need optional global messaging via `AlertBanner`.
- When you need secondary navigation or contextual content in a `Sidebar`.
- When you want mobile-specific sidebar behavior (`hidden` or `above-grid`) without building custom layout logic.

## Peer dependencies

- `@emotion/react`
- `react`
- `react-dom`
- `typescript`

See the `peerDependencies` section of `package.json` for compatible versions.

See [custom component build](#custom-component-build) for usage without React/Emotion.

## Example usage

<SandboxReact componentName={componentName} componentTsx={componentTsx} />

```tsx
import { Grid, Item } from '@guardian/stand/grid';
import { Layout, Sidebar } from '@guardian/stand/layout';
import { TopBar, TopBarToolName } from '@guardian/stand/TopBar';

<Layout>
	<TopBar>
		<TopBarToolName name="Layout Demo" favicon={{ letter: 'L' }} />
	</TopBar>
	<Sidebar layoutSmBreakpoint="above-grid">
		<div style={{ padding: '8px' }}>Sidebar content</div>
	</Sidebar>
	<Grid>
		<Item size={{ sm: 12, md: 8 }}>Main content</Item>
		<Item size={{ sm: 12, md: 4 }}>Secondary content</Item>
	</Grid>
</Layout>;
```

## Composition

`Layout` looks through its `children` and only renders the first child of each supported type:

| Region       | Accepted child type |
| ------------ | ------------------- |
| Alert banner | `AlertBanner`       |
| Top section  | `TopBar`            |
| Sidebar      | `Sidebar`           |
| Main content | `Grid`              |

Children that are not one of the supported types are ignored.

This means you can compose the shell declaratively, and `Layout` will place each supported child in the correct grid area.

## Layout Props

| Name           | Type                                     | Required | Default | Description                                                                                |
| -------------- | ---------------------------------------- | -------- | ------- | ------------------------------------------------------------------------------------------ |
| `children`     | `React.ReactNode`                        | No       | N/A     | Content to render inside the component. Only supported child component types are rendered. |
| `theme`        | `LayoutTheme`                            | No       | N/A     | Theme token overrides for layout grid structure across breakpoints.                        |
| `cssOverrides` | `SerializedStyles \| SerializedStyles[]` | No       | N/A     | Custom CSS styles.                                                                         |
| `className`    | `string`                                 | No       | N/A     | Additional class name(s).                                                                  |

## Sidebar Props

Use `Sidebar` as a child of `Layout` to fill the sidebar region.

| Name                 | Type                                     | Required | Default    | Description                               |
| -------------------- | ---------------------------------------- | -------- | ---------- | ----------------------------------------- |
| `children`           | `React.ReactNode`                        | No       | N/A        | Content to render inside the sidebar.     |
| `layoutSmBreakpoint` | `'above-grid' \| 'hidden'`               | No       | `'hidden'` | Sidebar behavior at the `sm` breakpoint.  |
| `theme`              | `SidebarTheme`                           | No       | N/A        | Theme token overrides for sidebar styles. |
| `cssOverrides`       | `SerializedStyles \| SerializedStyles[]` | No       | N/A        | Custom CSS styles.                        |
| `className`          | `string`                                 | No       | N/A        | Additional class name(s).                 |

### `layoutSmBreakpoint` behavior

- `hidden`: Sidebar is hidden on `sm` and appears again from `md` upward. This is shown in `TopBarGridSidebarHiddenSm`.
- `above-grid`: Sidebar is shown on `sm` and appears above the main grid content, then returns to the side from `md` upward. This is shown in `TopBarGridSidebarAboveGridSm`.

## Stories

### Top bar and grid layout

Use this as the baseline shell with primary navigation and main page content.

<Canvas of={TopBarGrid} />

### Top bar, grid and sidebar layout with sidebar hidden at sm breakpoint

Use this when sidebar content is optional on small screens and surfaced elsewhere (for example, inside top navigation).

<Canvas of={TopBarGridSidebarHiddenSm} />

### Top bar, grid and sidebar layout with sidebar above the grid at sm breakpoint

Use this when sidebar content should remain visible on small screens but switch to a horizontal section.

<Canvas of={TopBarGridSidebarAboveGridSm} />

### Alert banner, top bar, grid and sidebar layout with sidebar above the grid at sm breakpoint

Use this for global page-level status or messaging combined with full shell composition.

<Canvas of={AlertBannerTopBarGridSidebarAboveGridSm} />

## Customisation

We recommend using the Layout component as provided, but it can be customised using the `theme` or `cssOverrides` props as required.

### Custom theme

The `theme` prop allows you to override specific design tokens for the Layout component (for example grid templates at each breakpoint):

<Canvas of={CustomTheme} />

```tsx
import type { LayoutTheme } from '@guardian/stand/layout';
import { Layout } from '@guardian/stand/layout';

const customTheme: Partial<LayoutTheme> = {
	sm: {
		gridTemplateAreas: `'topbar' 'sidebar' 'grid'`,
		gridTemplateColumns: '1fr',
		gridTemplateRows: 'auto auto 1fr',
	},
};

const Component = () => <Layout theme={customTheme}>{...}</Layout>;
```

### CSS overrides

The `cssOverrides` prop allows you to pass custom CSS to the Layout component:

<Canvas of={CssOverrides} />

```tsx
import { Layout } from '@guardian/stand/layout';
import { css } from '@emotion/react';

const customStyles = css`
	border: 2px dashed #9aa0a6;
	border-radius: 8px;
	overflow: hidden;
`;

const Component = () => <Layout cssOverrides={customStyles}>{...}</Layout>;
```

## Custom Component Build

If you're not using React/Emotion, you can create a custom Layout component using the styles defined in the `LayoutTheme` type.

**`css`**

You can import the Layout styles as CSS from the package:

<SandboxCss
	componentName={componentName}
	componentHtml={componentHtml}
	componentCss={componentCss}
/>

**TypeScript/JavaScript**

Use the `componentLayout` variable and the `ComponentLayout` type:

<SandboxJs componentName={componentName} componentJs={componentJs} />
