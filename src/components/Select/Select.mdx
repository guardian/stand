import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import SelectStories, {
	Default,
	Fluid,
	Disabled,
	WithError,
	WithHelpText,
	CustomTheme,
	CssOverrides,
	RenderWithAsterisk,
	WithIdProp,
} from './Select.stories';
import {
	SandboxReact,
	SandboxCss,
	SandboxJs,
} from '../../util/storybook/sandbox/Sandbox';
import {
	componentName,
	componentCss,
	componentHtml,
	componentJs,
	componentTsx,
} from './sandbox';

<Meta of={SelectStories} />

# Select

A select component built on [React Aria](https://react-aria.adobe.com/Select).
The `Select` component combines a button with a popover containing `Option`s.
It provides validation error support, keyboard navigation and full accessibility out of the box.

## When to use

- Within a form to select an option from a menu

## Peer dependencies

- `@emotion/react`
- `react`
- `react-dom`
- `typescript`
- `react-aria-components`

See the `peerDependencies` section of `package.json` for compatible versions.

See [custom component build](#custom-component-build) for usage without React/Emotion.

## Example usage

<SandboxReact componentName={componentName} componentTsx={componentTsx} />

```tsx
import { Option, Select } from '@guardian/stand/Select';

/* types, if required */
import type {
	SelectProps,
	OptionProps,
	SelectTheme,
} from '@guardian/stand/Select';

// `id` prop as the selection value
<Select
	label="Select"
	onChange={(value) => console.log('Selected value:', value)}
>
	<Option id="opt-1">Option 1</Option>
	<Option id="opt-2">Option 2</Option>
	<Option id="opt-3">Option 3</Option>
</Select>;

// `children` as the selection value
<Select
	label="Select"
	onChange={(value) => console.log('Selected value:', value)}
>
	<Option>Option 1</Option>
	<Option>Option 2</Option>
	<Option>Option 3</Option>
</Select>;
```

## Props

| Name                 | Type                           | Required | Default | Description                                                                                                                                                           |
| -------------------- | ------------------------------ | -------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `label`              | `string`                       | Yes      | N/A     | Visible label rendered above the select.                                                                                                                              |
| `renderLabel`        | `(label: string) => ReactNode` | No       | N/A     | Custom render function for the label. Receives the `label` string and returns a ReactNode for visual enhancement                                                      |
| `contextualHelpText` | `string`                       | No       | N/A     | Optional contextual help text.                                                                                                                                        |
| `errorMessage`       | `string`                       | Yes      | N/A     | A validation error message.                                                                                                                                           |
| `placement`          | `Placement`                    | No       | N/A     | The placement of the options container element with respect to select input.                                                                                          |
| `shouldFlip`         | `boolean`                      | No       | N/A     | Whether the element options container element flip its orientation (e.g. top to bottom or left to right) when there is insufficient room for it to render completely. |
| `children`           | `React.ReactNode`              | No       | N/A     | Options to render in the dropdown, only `Option` components are accepted.                                                                                             |
| `theme`              | `SelectTheme`                  | No       | N/A     | Custom theme overrides.                                                                                                                                               |
| `cssOverrides`       | `SerializedStyles`             | No       | N/A     | Custom CSS styles.                                                                                                                                                    |
| `className`          | `string`                       | No       | N/A     | Additional class name(s).                                                                                                                                             |

## Option props

| Name       | Type          | Required | Default | Description                                                                                                                                                          |
| ---------- | ------------- | -------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `children` | `string`      | Yes      | N/A     | The visible label for the option. Also used as the selection value if `id` is not provided.                                                                          |
| `id`       | `string`      | No       | N/A     | An explicit identifier for the option. Used as the selection value when provided; falls back to `children`. This is the React Aria default way of setting the value. |
| `theme`    | `SelectTheme` | No       | N/A     | Custom theme overrides scoped to this option.                                                                                                                        |

### Setting the selection value

When an option is selected, React Aria emits the option's `id` as the value. Stand resolves this in priority order:

1. **`id` prop** - use this when you need an explicit identifier that differs from the display label.
2. **`children`** - when `id` is not provided, the text content is used as the value.

```tsx
// 1. id prop — onChange receives "option-1"
<Option id="option-1">Option 1</Option>

// 2. children fallback — onChange receives "Option 1"
<Option>Option 1</Option>
```

> **Note:** `children` must be a `string`. Passing JSX nodes is not supported because the value must be serialisable.

`Select` also accepts all props from the underlying [React Aria `Select` component](https://react-aria.adobe.com/Select).

## Stories

### Default

<Canvas layout="padded" of={Default} />

### Fluid

<Canvas layout="padded" of={Fluid} />

### Disabled

<Canvas layout="padded" of={Disabled} />

### With Error

<Canvas layout="padded" of={WithError} />

### With Help Text

<Canvas layout="padded" of={WithHelpText} />

### With Custom Rendered Label

<Canvas layout="padded" of={RenderWithAsterisk} />

### With Id Prop

<Canvas layout="padded" of={WithIdProp} />

## Customisation

We recommend using the Select component as provided, but it can be customised using the `theme` or `cssOverrides` props as required.

### Custom theme

The `theme` prop allows you to override specific design tokens for the Select component:

```tsx
import type { SelectTheme } from '@guardian/stand/Select';
import { Option, Select } from '@guardian/stand/Select';
import { baseColors } from '@guardian/stand';

const customTheme: Partial<SelectTheme> = {
	label: {
		color: baseColors.coolPurple[300],
	},
	button: {
		color: baseColors.coolPurple[200],
		backgroundColor: baseColors.coolPurple[800],
	},
};

const Component = () => (
	<Select label="Select" theme={customTheme}>
		<Option>Option</Option>
	</Select>
);
```

<Canvas layout="padded" of={CustomTheme} />

### CSS overrides

The `cssOverrides` prop allows you to pass custom CSS to the Select component:

```tsx
import { Select } from '@guardian/stand/Select';
import { css } from '@emotion/react';
import { baseColors } from '@guardian/stand';

const customStyles = css\`
button {
	color: ${baseColors.red[300]};
	border: 3px solid ${baseColors.red[800]};
}

\`;

const Component = () =>
	<Select label="Select" cssOverrides={customStyles}>
		<Option>Option</Option
	</Select>;
```

<Canvas layout="padded" of={CssOverrides} />

## Custom Component Build

If you're not using React/Emotion, you can create a custom Select component using the styles defined in the `SelectTheme` type. You should use the default `option` styling, as styling these elements only has [limited browser availability](https://developer.mozilla.org/en-US/docs/Learn_web_development/Extensions/Forms/Customizable_select).

**`css`**

You can import the Select styles as CSS from the package:

<SandboxCss
	componentName={componentName}
	componentHtml={componentHtml}
	componentCss={componentCss}
/>

**TypeScript/JavaScript**

Use the `componentSelect` variable and the `ComponentSelect` type:

<SandboxJs componentName={componentName} componentJs={componentJs} />
