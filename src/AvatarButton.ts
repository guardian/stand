/**
 * Avatar button component entry point
 *
 * Peer dependencies required to use these components:
 * - `@emotion/react`
 * - `react`
 * - `react-dom`
 * - `react-aria-components`
 * - `typescript`
 *
 * See the `peerDependencies` section of package.json for compatible versions.
 *
 * If you only need the built CSS (./component/avatar.css),
 * you don't need to install these.
 */
export { AvatarButton } from './components/AvatarButton/AvatarButton';
export type { AvatarButtonProps } from './components/AvatarButton/types';
export type { PartialAvatarButtonTheme as AvatarButtonTheme } from './components/AvatarButton/styles';
