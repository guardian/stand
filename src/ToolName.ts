/**
 * Tool Name component entry point
 *
 * Peer dependencies required to use these components:
 * - `@emotion/react`
 * - `react`
 * - `react-dom`
 * - `typescript`
 *
 * See the `peerDependencies` section of package.json for compatible versions.
 *
 * If you only need the built CSS (./component/toolName.css),
 * you don't need to install these.
 */
export { ToolName } from './components/ToolName/ToolName';
export type { ToolNameProps } from './components/ToolName/types';
export type { ToolNameTheme } from './components/ToolName/styles';
export { componentToolName } from './styleD/build/typescript/component/toolName';
export type { ComponentToolName } from './styleD/build/typescript/component/toolName';
