---
'@guardian/stand': patch
---

Update `Typography` component to be based on react-aria-components `Text` component for better compatibility with other react aria based components. This change also adds `react-aria-components` as a peer dependency to the component.
